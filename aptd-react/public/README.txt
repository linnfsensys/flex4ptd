These files and directories must be duplicated under 
	src/assets
	public
The former directory is used for production and the latter for development.  
I could not get it to work using just 1 directory for image assets so far.
The final results appear in build/imageDir in one flat directory, for production.

